version: '3.8'
services:
  router:
    build:
      context: .
      dockerfile: router/Dockerfile
    ports:
      - "5555:5555"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
